{
  "routes": [
    {
      "method": "POST",
      "path": "/custom",
      "handler": "custom.createData",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/custom",
      "handler": "custom.getData",
      "config": {
        "policies": []
      }
    }
  ]
}
